// Ternary operatorlar
let temperature = 35;

let howa =
    temperature < 0
        ? "sowuk"
        : temperature > 10 && temperature < 30
        ? "yssy"
        : "Belli dal";

console.log("Howa", howa);
